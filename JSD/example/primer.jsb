app {
  model Covek {

      properties {
        zanimanje : Zanimanje : ArrayList : @ManyToMany
      }

      controller : "getName()","getAge()"
  }

   model Zanimanje {


      controller : "getName()","getAge()"
  }
}