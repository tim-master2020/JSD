import { Component, OnInit } from '@angular/core';
import { Validators, FormBuilder, FormGroup } from '@angular/forms';
import { ActivatedRoute, Router } from '@angular/router';
import { Service } from '../app.service';
import { {{model.name}}PreviewGeneratedComponent } from '../generated/{{model.name.lower()}}-generated/{{model.name}}PreviewGenerated';


@Component({
  selector: 'app-{{model.name}}-show',
  templateUrl: '../generated/{{model.name.lower()}}-generated/{{model.name}}PreviewGenerated.html',
  styleUrls: []
})
export class {{model.name}}PreviewComponent extends {{model.name}}PreviewGeneratedComponent implements OnInit {

  constructor(
    public service : Service,
    public formBuilder : FormBuilder,
    public router : Router,
    public activatedRoute : ActivatedRoute
  ) { 
      super (service, formBuilder, router, activatedRoute);
    }

  ngOnInit() {
    
  }

}