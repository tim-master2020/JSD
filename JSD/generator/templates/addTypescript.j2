import { Component, OnInit } from '@angular/core';
import { Validators, FormBuilder, FormGroup } from '@angular/forms';
import { ActivatedRoute, Router } from '@angular/router';
import { Service } from '../app.service';
import { Add{{model.name}}GeneratedComponent } from '../generated/{{model.name.lower()}}-generated/{{model.name}}Generated';

@Component({
  selector: 'app-{{model.name}}-add',
  templateUrl: '../generated/{{model.name.lower()}}-generated/{{model.name}}Generated.html',
  styleUrls: []
})
export class Add{{model.name}}Component extends Add{{model.name}}GeneratedComponent implements OnInit  {


  constructor(
    public service : Service,
    public formBuilder : FormBuilder,
    public router : Router,
    public activatedRoute : ActivatedRoute
  ) { 
    super(service, formBuilder, router, activatedRoute);
  }

  ngOnInit() {

  }

}
